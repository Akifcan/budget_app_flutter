import 'package:flutter/material.dart';

const primaryColor = Color(0xff006994);
const body = Color(0xffdedede);

ThemeData theme =
    ThemeData(scaffoldBackgroundColor: body, primaryColor: primaryColor);
